spring:
  application:
    name: order-service

  jackson:
    serialization:
      # Serialize Instant as ISO-8601, not a Unix timestamp: outbox payloads are read by other services
      write-dates-as-timestamps: false

  mvc:
    # RFC 7807 responses for Spring's own MVC errors (validation 400, method not allowed 405)
    problemdetails:
      enabled: true

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:order_db}
    username: ${DB_USERNAME:order_user}
    password: ${DB_PASSWORD:order_pass}
    hikari:
      # keepalive-time must stay below Aurora Serverless NAT idle timeout (350s)
      keepalive-time: 240000
      connection-timeout: 30000
      maximum-pool-size: 10

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false

  cloud:
    aws:
      region:
        static: ${AWS_REGION:eu-west-1}
      credentials:
        instance-profile: true

order:
  outbox:
    poll-delay-ms: 5000
    sqs:
      queue-name: order-events

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when-authorized

logging:
  level:
    com.aubin.order: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: INFO
